# Dependencies for both the local (Ollama) app and the Vercel (Gemini) deployment.
# Vercel Python runtime installs these automatically from requirements.txt.

# --- Local FastAPI app (Ollama) ---
fastapi==0.115.*
uvicorn[standard]==0.32.*
jinja2==3.1.*
httpx==0.28.*

# --- Shared / Vercel (OpenRouter) ---
psycopg[binary]==3.2.*
psycopg-pool==3.2.*
python-dotenv==1.0.*
beautifulsoup4==4.12.*
crewai==1.15.*
